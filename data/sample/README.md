# Sample Data

이 폴더의 `asset_beta_panel.csv`는 baseline 계산을 검증하기 위해 만든 합성 데이터입니다.

## Columns

- `asset`
- `beta`
- `expected_excess_return`
- `volatility`

## Usage

```bash
python -m src.run_baseline
```

